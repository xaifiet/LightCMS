{%  set size = (form.size.vars.value == '') ? '4' : form.size.vars.value %}
<div class="col-md-{{ size }} widgetform" id="widget-{{ form.vars.id }}">
    {{ form_row(form._type) }}
    {{ form_row(form.position) }}
    {{ form_row(form.size) }}
    <input type="hidden" name="{{ form.content.vars.full_name }}" value="toto" id="{{ form.content.vars.id }}" />
    <div class="box box-primary">
        <div class="box-header with-border">
            <span class="fa fa-arrows sorthandle"></span>
            <h3 class="box-title">Widget</h3>
            <div class="btn-group pull-right" data-keep="yes">
                <button type="button" class="btn btn-xs btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                    <i class="glyphicon glyphicon-cog"></i>
                    <span class="fa fa-caret-down"></span>
                </button>
                <ul class="dropdown-menu" role="menu">
                    <li><a href="#" data-bind="@click[setWidgetSize(4,col-md-4)]">1/3 Size</a></li>
                    <li><a href="#" data-bind="@click[setWidgetSize(6,col-md-6)]">1/2 Size</a></li>
                    <li><a href="#" data-bind="@click[setWidgetSize(8,col-md-8)]">2/3 Size</a></li>
                    <li><a href="#" data-bind="@click[setWidgetSize(12,col-md-12)]">Full Size</a></li>
                    <li role="presentation" class="divider"></li>
                    <li><a href="#" data-bind="click[loadModal(#{{ form.vars.id }}-widgetmodal,{{ lcmspath('node', 'widget', 'edit', {'id': 'widget-'~form.vars.id}) }})]">Edit Widget</a></li>
                    <li><a href="#" data-bind="@click[deleteCollectionRow(#widget-{{ form.vars.id }})]">Delete Widget</a></li>
                </ul>
            </div>
        </div>
        <div class="box-body">
            {% if form.content.vars.value == '' %}
                Heading
                Donec id elit non mi porta gravida at eget metus. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus. Etiam porta sem malesuada magna mollis euismod. Donec sed odio dui.

                View details »
            {% else %}
                {{ form.content.vars.value|raw }}
            {% endif %}
        </div>
        <div id="{{ form.vars.id }}-widgetmodal" class="modal fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
            <div class="modal-dialog">
                <div class="modal-content"></div>
            </div>
        </div>
    </div>
</div>