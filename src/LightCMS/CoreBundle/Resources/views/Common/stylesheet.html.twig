{% block stylesheets %}
    {% stylesheets
        '@fontawesome'
        '@bootstrap_css'
        '@adminlte_css'
        '@summernote_css'
        '@treegrid_css'
    %}
    <link rel="stylesheet" href="{{ asset_url }}" />
    {% endstylesheets %}
    {% for stylesheet in lcmscss(display) %}
        <link rel="stylesheet" href="{{ asset(stylesheet) }}" />
    {% endfor %}
{% endblock %}