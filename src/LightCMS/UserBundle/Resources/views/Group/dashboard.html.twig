{% extends 'LightCMSCoreBundle:Backend:layout.html.twig' %}

{% block body %}
    <section class="content-header">
        <h1>Group List
            <div class="pull-right">
                <div class="btn-group" style="margin-right:20px">
                    <a href="{{ lcmspath('user', 'user', 'create', {}) }}" class="btn btn-sm btn-success"><i class="fa fa-plus"></i></a>
                </div>
                <div class="btn-group" style="margin-right:20px">
                    <div class="pull-right" style="width: 300px">
                        <div class="input-group">
                            <span class="input-group-addon">Filter</span>
                            <input id="filter" type="text" class="form-control" placeholder="Type here..." data-bind="ready{setFocus()}|keyup{filter()}">
                        </div>
                    </div>
                </div>
            </div>
        </h1>
    </section>
    <section class="content">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th style="width: 50px">#</th>
                                <th>name</th>
                                <th style="width: 100px">Action</th>
                            </tr>
                        </thead>
                        <tbody class="searchable">
                            {% for group in groups %}
                                <tr>
                                    <td>{{ group.id }}</td>
                                    <td>{{ group.name }}</td>
                                    <td>
                                        <a class="btn-sm btn-info" href="{{ lcmspath('user', 'group', 'edit', {'id': group.id }) }}"><i class="fa fa-pencil-square-o"></i></a>
                                        <a class="btn-sm btn-danger" href="{{ lcmspath('user', 'group', 'delete', {'id': group.id }) }}"><i class="fa fa-trash"></i></a>
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </section>
{% endblock %}