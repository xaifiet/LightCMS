{% extends 'LightCMSCoreBundle:Backend:layout.html.twig' %}

{% form_theme form 'LightCMSCoreBundle:Form:theme.html.twig' %}

{% block body %}
    {{ form_start(form) }}

    <section class="content-header">
        <h1>
            {% if entity.getId() %}
                {{ entity.name }}
            {% else %}
                Create Group
            {% endif %}

            <div class="pull-right">
                <div class="btn-group" style="margin-right:20px">
                    {{ form_row(form.submit) }}
                </div>
                <div class="btn-group" style="margin-right:20px">
                    <a href="{{ lcmspath('user', 'group', 'dashboard', {}) }}" class="btn btn-default">Back</a>
                </div>
            </div>
        </h1>
    </section>

    <section class="content">
        <div class="box box-primary">
            <div class="box-header with-border">
                <h3 class="box-title">Group informations</h3>
            </div>
            <div class="box-body">
                {{ form_row(form.name) }}
            </div>
        </div>
    </section>

    {{ form_end(form) }}
{% endblock %}

